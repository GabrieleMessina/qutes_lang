// // Qubit resuse test, working.
// qubit a = 0q;
// hadamard a;
// a = 0q; //prev referenced qubit should be uncomputed and freed. this woul still be a different qubit then before, because the free happens at assign while new register of right operand happens before.
//         // actually, that operation (hadamard a) is nosense, we could just drop it at compile time. But is out of scope for now.
// pauliy a;
// qubit b = 0q; //this shold point to the first qubit.
// pauliz b;

// // Qubit resuse test, working.
// qubit a = |0>;
// hadamard a;
// a = |0>; //prev referenced qubit should be uncomputed and freed. this woul still be a different qubit then before, because the free happens at assign while new register of right operand happens before.
//         // actually, that operation (hadamard a) is nosense, we could just drop it at compile time. But is out of scope for now.
// pauliy a;
// qubit b = |0>; //this shold point to the first qubit.
// pauliz b;

// // Test parallel for loop with array access.
// qubit a = 0q;
// qubit[] v = [0q, 0q, 0q, 0q];
// for e,i in v parallel{
//     print e;
//     hadamard v[i];
// }


// // Error, trying to access array element multiple times.
// qubit[] v = [0q, 0q, 0q, 0q];
// for e,i in v[0..v.length()-1] parallel{
//     print e;
//     print v[i+1];
// }

// // No error thrown.
// qubit[] v = [0q, 0q, 0q, 0q];
// for e,i in v[0..v.length()-1:2] parallel{
//     print e;
//     print v[i*2+1];
// }

// quint[] v = [1q, |+>, 3q];
// qubit[] res = [0q]*v.length(); 
// for i in [0..v.length()-1]{
// 	res[i] = v[i] > v[i+1];
// }
// measure res;


////Currently not working.
// qubit q = 1q;
// measure q;

// qubit[] v = [1q, 0q, 0q, 0q];
// int n = v.length();

// qubit[] res = [0q, 0q, 0q, 0q];
// qubit[] res2 = [0q, 0q, 0q, 0q];
// for i in [0..n/2]{
//     print i;
//     res[i] = (v[i] > v[i+1]);
// }
// for i in [0..n-1:2]
// res2[i] = (v[i] < v[i+1]);
// measure res;
// measure res2;
// measure v;

// for i in res[n..0:-1] {
//     print i;
//     measure i;
// }

//// Test shift operator on array.
// quint[] v = [1q, 0q, 0q, 0q];
// v[0] = 0q;
// v >> 1;
// measure v;

//// Comparison operators on quints.
// quint a = 1q;
// quint b = 1q;
// qubit eq = a==b;
// qubit neq = a!=b;
// qubit gr = a>b;
// qubit lo = a<b;
// qubit ge = a>=b;
// qubit le = a<=b;
// measure eq;
// measure neq;
// measure gr;
// measure lo;
// measure ge;
// measure le;


//// Array literal, slicing, casting and access. 
// quint[] v = [1q, |+>, 3q];
// quint[] res = [0q]*v.length(); 
// quint[] res = [0q]; 
// res[0] = v[0];
// for i in [0..v.length()]{
//     res[i] = v[i];
// }
// print res;
// print v;
// measure res;
// measure v;


// int[] v = [1, 2, 3];
// int a = v.length();
// function f = v.length;
// quint[] res = [1q]*v.length();
// quint[] res2 = [1q]*a;
// print f;
// measure res2;

// // Array slicing and access.
// int a = 2;
// int[] v = [1, 2, 3];
// int[] w1 = v[..a]; // [1, 2]
// int[] w2 = v[1..a]; // [2]
// int[] w3 = v[a..]; // [3]
// print w2;

//// for yielding transformed input.
// int[] a = [1, 2, 3];

// quint[] b = for i in a yield foo(i);
// measure b;

// quint foo(int x){
//     quint y = x;
//     return y;
// }

////Test function call and return value.
// qubit _ = |0>;
// measure _;

// int n = 3;
// qubit foo(){
//     return n+1;
// }

// qubit b = foo();
// measure b;


// //Classical Array product by scalar
// int n = 3;
// int[] a = [n, n];
// print n;
// print a;

// qubit _ = |0>;
// measure _;


////Quantum Array product by scalar
// quint n = 3q;
// quint[] a = [n] * 3;
// print n;
// print a;

// measure n;
// measure a;


////qubit literal not cast.
// qubit a = true q;
// qubit b = {0.3, -3.9}q;
// measure b;
// qubit bb = {true, true}q;
// qubit c = {.7, .3}q;
// qubit cc= {0.7}q;
// qubit d = |+>;
// qubit dd = |->;
////qubit literal from cast.
// qubit e = {0, 1}q; //from quint but we take only first two states.
// qubit f = 1q; //from quint but we take only first two states.
// qubit g = false; //from bool.
////qubit cast not allowed.
//qubit h = 0..3; //from range
//qubit i = 1; //from int.

////quint literal not cast.
// quint l = 3q;
// quint m = {1, 2, 3}q;
// quint mm = {0..3}q;
////quint literal from cast.
// quint n = |0>; //from all qubits.
// quint o = 3; //from int.
// quint p = 1..3; //from range.

// //Array literal, slicing, casting and access.
// qubit _ = |0>;
// quint addr = {0..3}q;
// quint addr2 = 0..3;
// int[] a = [1..5];
// int[] aa = [1..5:2];
// int[] b = a[1..3];
// int[] bbb = a[0..3:2];
// quint[] aa = [1..5];
// quint[] bb = aa[1..3];
// quint[] c = a;
// quint d = aa[addr];
// print a;
// print b;
// print c;

// measure d;
// measure addr;
// measure addr2;
// measure c;
// measure o;
// measure p;



// quint a = 1..4;
// int b = 1..4;
// quint[] c = [1..4];
// int[] d = [1..4];
// measure a;
// measure c;
// print b;
// print c;
// print d;

// for i in [0..3]{
//     print i;
// }


// quint[] v = [1q, 3q, 0q];
// // quint a = v[1];
// // quint i = [0,1,2,3]q;
// // quint b = v[i];
// quint j = [0..3]q;
// quint c = v[j];
// // measure a;
// measure j;
// measure c;
// measure v[0];



// qubit a = 0q;
// qubit b = 0q;

// propagation(a, b);

// measure a;
// measure b;

// void propagation(qubit a, qubit b){
//     qubit[] q = [a, 0q, 0q, 0q, 0q, b];
//     for ii in [0, 2, 4]{
//         entangle(q[ii], q[ii + 1]);
//     }
//     for i in [0, 2]{
//         hadamard_measure(q[i + 1],q[i + 2]);
//         if(q[i + 1]) pauliz q[i];
//         if(q[i + 2]) not q[i + 3];
//     }
// }

// void entangle(qubit a1, qubit b1){
//     hadamard a1;
//     mcx a1, b1;
// }
// void hadamard_measure(qubit a2, qubit b2){
//     mcx a2, b2;
//     hadamard a2;
// }


// qubit[] a = [|+>,|+>,|+>];
// qubit d = 0q;
// qubit e = 0q;
// void foo(qubit[] b, qubit c){
//     hadamard b;
//     mcx b, c;
// }
// foo(a,d);
// measure e;


// qubit[] input = [|+>,|+>,|+>];
// qubit output = |->;

// void function(qubit[] _input, qubit _output){
//     // for q in _input {
//     //     mcx q, _output;
//     // }
//     hadamard _output;
//     for q in _input {
//         id q;
//     }
//     hadamard _output;
// }

// function(input, output);

// hadamard input;
// qubit isConstant = false;
// for q in input{
//     isConstant or not q;
// }
// measure isConstant;



// qubit[] bitmask = [1q, 0q, 1q];
// quint[] vector = [1q, 2q, 3q];
// for q,i in vector{
//     if(bitmask[i]){
//         hadamard q;
//     }
// }
// measure vector;

// measure vector[0];
// measure vector[1];
// measure vector;


// qustring array = "1110111";
// qustring pattern = "00";
// qubit found = 0q;
// if(pattern in array){
// 	not found;
// }
// else{
// 	hadamard found;
// }
// measure found;


//hadamard input;
//qubit isConstant = false;
//for q in input{
//    isConstant = isConstant or not q;
//}
//measure isConstant;

// qubit a = 0q;
// qubit b = 0q;
// entangle(a, b);
// entangle(a, b);
// measure a;
// measure b;

// void entangle(qubit a, qubit b){
//     hadamard a;
//     mcx a, b;
// }


// qubit b = 0q;
// void foo(qubit a ){
//     hadamard a;
// }
// measure b;

// qubit a = 0q;
// qubit b = 0q;

// propagation(a, b);

// measure a;
// measure b;

// void propagation(qubit a, qubit b){
//     qubit[] q = [a, 0q, 0q, 0q, 0q, b];
//     for i in [0, 2, 4]
//         entangle(q[i], q[i + 1]);
//     for i in [0, 2]{
//         hadamard_measure(q[i + 1],q[i + 2]);
//         measure q[i + 1];
//         measure q[i + 2];
//         if(q[i + 1]) pauliz q[i];
//         if(q[i + 2]) not q[i + 3];
//     }
// }

// void entangle(qubit a1, qubit b1){
//     hadamard a1;
//     mcx a1, b1;
// }
// void hadamard_measure(qubit a2, qubit b2){
//     mcx a2, b2;
//     hadamard a2;
// }